node_modules/
dist/
build/

# Environment — never commit secrets
.env
.env.local
.env.production
.env.*.local

# Logs
logs/*.log
npm-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/settings.json
*.swp

# Coverage
coverage/
.nyc_output/

# Security — never commit keys or certificates
*.pem
*.key
*.crt
*.p12
*.pfx
secrets/
backups/
.scratch/

# ⚠️ Development OTP outbox — holds live one-time codes. Never commit.
.otp-outbox.json

# Speech-to-text model (hundreds of MB) — fetched per environment by
# `npm run models:fetch:whisper`, pinned by models/whisper-manifest.json.
models/onnx-community/

# Encrypted patient files (voice-note audio). Never commit, never serve statically.
storage/
